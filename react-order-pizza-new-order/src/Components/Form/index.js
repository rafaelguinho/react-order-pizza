import styled from "styled-components/macro";

const Form = styled.form`
  padding: 1em 4em;
  display: grid;
  > .button-container {
    display: flex;
    justify-content: center;
    margin-top: 1em;
  }
`;

export default Form;